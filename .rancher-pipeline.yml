stages:
- name: a
  steps:
  - publishImageConfig:
      dockerfilePath: ./dockerdemo/Dockerfile
      buildContext: .
      tag: dockerdemo
      pushRemote: true
      registry: registry.cn-hangzhou.aliyuncs.com
    when:
      event:
        include:
        - pull_request
  when:
    event:
      include:
      - pull_request
timeout: 60
branch: {}
notification: {}
